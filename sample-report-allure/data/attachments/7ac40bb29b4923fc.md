# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: demo-intentional-failure.spec.ts >> Demo - intentional failure (diagnostics showcase) >> [INTENTIONAL FAILURE] homepage heading does not really say this
- Location: tests\demo-intentional-failure.spec.ts:11:3

# Error details

```
Error: this assertion is deliberately wrong to demonstrate failure diagnostics

expect(locator).toHaveText(expected) failed

Locator:  locator('h1').first()
Expected: "This text will never match - demonstrating failure diagnostics"
Received: "For the moments that matter most"
Timeout:  10000ms

Call log:
  - this assertion is deliberately wrong to demonstrate failure diagnostics with timeout 10000ms
  - waiting for locator('h1').first()
    23 × locator resolved to <h1 class="home-heading">For the moments that matter most</h1>
       - unexpected value "For the moments that matter most"

```

```yaml
- heading "For the moments that matter most" [level=1]
```

# Test source

```ts
  1  | import { expect, test } from '@src/fixtures/base.fixture';
  2  | import { withHighlight } from '@src/utils/screenshot';
  3  | 
  4  | /**
  5  |  * Deliberately failing test, kept separate from the real TC1-TC6 specs so its failure is never
  6  |  * mistaken for a genuine product bug. Exists solely to demonstrate the diagnostics pipeline
  7  |  * required by the assignment: a screenshot at the point of failure, clear failure detail in the
  8  |  * HTML report, and a visual highlight of the element the (intentionally wrong) assertion targeted.
  9  |  */
  10 | test.describe('Demo - intentional failure (diagnostics showcase)', () => {
  11 |   test('[INTENTIONAL FAILURE] homepage heading does not really say this', async ({
  12 |     home,
  13 |     page,
  14 |   }, testInfo) => {
  15 |     await home.goto();
  16 | 
  17 |     const heading = page.locator('h1').first();
  18 |     await withHighlight(page, testInfo, heading, 'homepage h1', async () => {
  19 |       await expect(
  20 |         heading,
  21 |         'this assertion is deliberately wrong to demonstrate failure diagnostics',
> 22 |       ).toHaveText('This text will never match - demonstrating failure diagnostics');
     |         ^ Error: this assertion is deliberately wrong to demonstrate failure diagnostics
  23 |     });
  24 |   });
  25 | });
  26 | 
```